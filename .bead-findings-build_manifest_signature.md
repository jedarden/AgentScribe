# build_manifest_from_events Function Signature Documentation

## Location
**File:** `src/index.rs`  
**Line:** 627  
**Function:** `build_manifest_from_events`

## Function Signature
```rust
pub fn build_manifest_from_events(
    events: &[Event],
    session_id: &str,
    source_agent: &str,
    project: Option<&str>,
    model: Option<&str>,
    parent_session_id: Option<&str>,
) -> SessionManifest
```

## Parameters (6 total)
1. **events:** `&[Event]` - Slice of scraped events
2. **session_id:** `&str` - Session identifier string
3. **source_agent:** `&str` - Name of the source agent
4. **project:** `Option<&str>` - Optional project path
5. **model:** `Option<&str>` - Optional LLM model name
6. **parent_session_id:** `Option<&str>` - Optional parent session identifier

## Purpose
Builds a minimal `SessionManifest` from scraped events for indexing, creating metadata directly from events without enrichment data (summary, outcome, etc.) which is added later.

## Return Type
`SessionManifest` struct containing:
- session_id (String)
- source_agent (String)
- project (Option<String>)
- started (DateTime<Utc>)
- ended (Option<DateTime<Utc>>)
- turns (u32)
- summary (None at this stage)
- outcome (None at this stage)
- tags (Vec<String>)
- files_touched (Vec<String>)
- model (Option<String>)
- parent_session_id (Option<String>)

## Confirmed
✓ Function located at line 627 in src/index.rs
✓ Takes exactly 6 parameters
✓ Includes `parent_session_id: Option<&str>` as the 6th parameter
✓ Parameter order matches specification
