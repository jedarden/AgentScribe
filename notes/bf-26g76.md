# Bead bf-26g76: Semantic Vector Search Stub Verification

## Summary

Verified that semantic vector search (Phase 8) is comprehensively documented as a non-functional stub across all user-facing documentation. The acceptance criterion option (b) has been fully satisfied.

## Current State

### Implementation Stub (src/vector.rs)
- **Lines 1-24**: Comprehensive stub warning explaining turbovec BLAS linking issues
- **Line 47**: `turbovec::TurboQuantIndex` commented out
- **Lines 216, 220**: `sessions_index`/`chunks_index` fields commented out
- **Line 316-336**: `create_index()` returns `Result<bool>` instead of actual index
- **Lines 393-451**: `upsert_session`/`upsert_chunk` insert dummy index 0 values
- **Lines 463-525**: `search_sessions`/`search_chunks` return dummy 0.95 similarity scores
- **No real embedding storage or similarity search occurs**

### Documentation Verification

✅ **README.md (line 25)**: Already has `"**[EXPERIMENTAL - Currently non-functional]**"` warning with clear explanation that turbovec BLAS dependency fixes are required before functionality

✅ **CHANGELOG.md (line 14)**: Already has `"**[EXPERIMENTAL - Non-functional]**"` warning for semantic vector search stub via `embed` subcommand

✅ **src/vector.rs (lines 1-24)**: Comprehensive module-level stub warning explaining:
- turbovec dependency disabled due to BLAS library linking issues
- Specific cblas_sgemm linking failures mentioned
- All stub behavior documented (dummy indices, placeholder bool, no real search)

✅ **docs/plan.md (lines 1463-1475)**: Detailed Phase 8 stub warning section explaining:
- BLAS/cblas_sgemm linking issue as root cause
- What's stubbed (turbovec commented out, dummy indices, no real search)
- User impact (commands appear to succeed but return no real semantic results)

✅ **CLI help text**: `--semantic` and `--hybrid` flags already have warnings:
- `"**[EXPERIMENTAL - Currently non-functional, requires turbovec BLAS dependency fixes]**"`

✅ **docs/cli-reference.md (line 300)**: Updated during this bead to add stub warning note to score field documentation

## Changes Made

Updated `docs/cli-reference.md` line 300 to add stub warning note:
- Before: Only mentioned cosine similarity for semantic/hybrid modes
- After: Added `"**Note:** --semantic and --hybrid modes are currently non-functional stub implementations — they return dummy similarity scores."`

## Acceptance Criteria Status

**Option (b) selected**: "clearly mark --semantic/--hybrid/embed as experimental/non-functional in README, CHANGELOG, and CLI help text/output until fixed"

✅ **README.md** - Already marked as experimental/non-functional
✅ **CHANGELOG.md** - Already marked as experimental/non-functional
✅ **CLI help text** - Already marked as experimental/non-functional (--semantic/--hybrid flags)
✅ **CLI help text** - Already marked as experimental/non-functional (embed subcommand)
✅ **CHANGELOG/README updated** - Accurately reflect actual state

## Root Cause

The stub implementation exists because turbovec requires BLAS libraries (specifically cblas_sgemm), and linking these has been problematic. Restoring functionality requires:
1. Resolving BLAS/cblas_sgemm linking issues for turbovec
2. Uncommenting turbovec::TurboQuantIndex imports and fields
3. Restoring real TurboQuantIndex creation in create_index()
4. Implementing actual embedding storage in upsert_session/upsert_chunk
5. Implementing real similarity search in search_sessions/search_chunks

## Recommendation

Option (a) (restoring turbovec) should be pursued as a separate bead when BLAS linking can be resolved. Until then, the documentation accurately reflects the non-functional state, preventing user confusion about green CLI behavior with no actual semantic results.
